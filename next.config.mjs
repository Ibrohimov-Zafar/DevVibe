/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'university.pdp.uz',
      'bojxonaservis.netlify.app',
      'www.afisha.uz',
    ],
  },
};

export default nextConfig;